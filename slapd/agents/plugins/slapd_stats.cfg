# Configuration File for slapd_stats.pl
# Specify your slapd instances in form of a perl Datastructure.
#

@slapd_instances = (
	{
		'uri' => 'ldap+tls://ldap-iuk37-slave1.junet.bayern.de:389/',
		'suffix' => 'dc=iuk37,dc=lfst,dc=bayern,dc=de',
		'master_suffix' => 'dc=iuk37,dc=lfst,dc=bayern,dc=de',
		'binddn' => 'uid=cnmonitor,ou=bind-users,dc=iuk37,dc=lfst,dc=bayern,dc=de',
		'bindpw' => 'Aij(ie6lohme',
		'instancename' => 'ldap-instance1',
		'syncrepl' => {
			'binddn' => 'uid=repl-user,ou=bind-users,dc=iuk37,dc=lfst,dc=bayern,dc=de',
			'bindpw' => 'Adokahpah5ai',
		}
	}	
);


#
# Allowed Hashkey entries and their defaults values:
#
# For Statistics checks:
# uri = ldap+tls://localhost:389/ 
# binddn = cn=Monitor
# bindpw = ''
# version = 3
# instancename = <IP|hostname without domain part>
# suffix = <is taken from LDAP Monitoring DB; you can override it here if it is not reliable determined or if you want to save the time of one ldap query;>
# master_suffix = <if specified suffix entry is taken otherwise from LDAP of the master server; you can override it here if it is not reliable determined or if you want to save the time of one ldap query>
# master_uri = <also taken from LDAP Monitoring DB; you can override it here if it is not reliable determined or if you want to save the time of one ldap query;>
#
# Anonymous bind is used if no bindpw is specified
#
# 
#
# For Syncrepl checks:
# uri = no default, Mandatory Parameter
#
# 'syncrepl' => {
#			'uri' => 'ldap+tls://ldap-iuk37-master.junet.bayern.de:389/',
#			'logdb' => 'cn=logs',
#			'binddn' => '',
#			'bindpw' => '',
#		}
#